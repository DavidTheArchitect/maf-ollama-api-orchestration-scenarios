"""Local MCP servers bundled with the Invocations API sample.

These servers run over stdio with no network access, no credentials, and no
writes. They exist so the MCP orchestration scenarios can teach tool usage on a
laptop without any external setup.
"""
